#include<bits/stdc++.h>
using namespace std;

int wgt[150],dp[150],num[150];

int solve(int n,int m){

    int i,j,k;

    dp[0] = 1;

    for(i=1;i<=m;i++){
        for(j=1;j<=m;j++){
            if(num[])
        }
    }
}


int main(){

    int i,j,k,x,y,z,a,b,c,cn=0,ts,cas=0,n,m;
    cin>>ts;

    while(++cas<=ts){

        memset(wgt,0,sizeof wgt);
        memset(dp,0,sizeof dp);
        memset(num,0,sizeof num);

        scanf("%d %d",&n,&m);

        for(i=1;i<=m;i++){
            scanf("%d",wgt[i]);
        }

    }
    return 0;
}
